<scroll-view>
  <view wx:for="{{itemArr}}" wx:key="{{*this}}">{{item.text}}
    <block wx:if="{{item.node.length > 0}}">
      <view wx:for="{{item.node}}" wx:key="{{*this}}">{{item.text}}
        <block wx:if="{{item.node.length > 0}}">
          <view wx:for="{{item.node}}" wx:key="{{*this}}">{{item.text}}
            <block wx:if="{{item.node.length > 0}}">
              <view wx:for="{{item.node}}" wx:key="{{*this}}">{{item.text}}</view>
            </block>
          </view>
        </block>
      </view>
    </block>
  </view>
</scroll-view>